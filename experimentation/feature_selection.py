import sklearn

'''Backward feature elimination
    Features are selected by starting with all possible features,
    building the model, and then looping through and removing 
    the features that perform the worst'''

# def backward_feature_elimination(all_features, Y = []):'''

